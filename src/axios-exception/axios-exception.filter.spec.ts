import { AxiosExceptionFilter } from './axios-exception.filter';

describe('AxiosExceptionFilter', () => {
  it('should be defined', () => {
    expect(new AxiosExceptionFilter()).toBeDefined();
  });
});
